# 02. Example product comparison

## Where this stands

This is a worked example of the layout, not real work.
One round is done: `01-shortlist/` holds the comparison as an `index.html`.
Nothing is open.

## What this example shows

A second worked example - the kind of task where the deliverable is a side-by-side comparison rendered as a small HTML site.

```
02.example-product-comparison/
├── CLAUDE.md        ← everything about this task, current position first (this file)
└── 01-shortlist/
    └── index.html   ← comparison table
```

The person can delete this example once the layout is familiar.
Claude never writes here.
