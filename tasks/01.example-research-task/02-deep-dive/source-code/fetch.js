// Example helper script. Anything the user might want to re-run lives in source-code/.
// Throwaway scratch files (raw HTML, debug dumps, Playwright .yml snapshots) go in ../temp/ instead.

console.log("replace me with a real helper");
